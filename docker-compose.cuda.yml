services:
  qwen3-tts:
    environment:
      QWEN3_TTS_BACKEND: cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
